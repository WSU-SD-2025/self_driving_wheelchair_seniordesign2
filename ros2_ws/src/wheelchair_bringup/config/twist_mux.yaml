twist_mux:
  ros__parameters:
    #output_topic: /cmd_vel

    
    topics:
      manual:
        topic: /cmd_vel/manual
        timeout: 0.3
        priority: 100

      nav:
        topic: /cmd_vel/nav
        timeout: 0.5
        priority: 10
